{% extends 'master.html.twig' %}

{%- block html -%}

    {% if app.environment == 'dev' %}

        {{ parent() }}

    {% else %}

        {%- spaceless -%}
            {{ parent() }}
        {%- endspaceless -%}

    {% endif %}

{%- endblock -%}
